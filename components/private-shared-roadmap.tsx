'use client';
import { useGetJsonQuery } from '@/lib/redux/api';
import { ShieldCheck,LogIn,AlertCircle } from 'lucide-react';
import { Button,Card } from '@/components/ui';
import { PublicRoadmapViewer } from './public-roadmap-viewer';
export function PrivateSharedRoadmap({slug,status}:{slug:string;status:number}){const {data, isLoading:loading}=useGetJsonQuery({url:`/api/shared/${slug}`,tag:`shared:${slug}`},{skip:status===401});const roadmap=(data as any)?.roadmap||null;if(roadmap)return <main className="min-h-screen p-6 md:p-10"><div className="mx-auto max-w-7xl"><div className="mb-4 rounded-2xl border border-primary/20 bg-primary/5 p-4 text-sm"><b>Private roadmap · Invite only.</b> This roadmap is available to the Roadmap accounts it was shared with.</div><PublicRoadmapViewer roadmap={roadmap} syncUrl={`/api/shared/${slug}?sync=1`} cloneUrl={undefined}/></div></main>;return <main className="grid min-h-screen place-items-center p-6"><Card className="w-full max-w-md p-7 text-center shadow-xl"><div className="mx-auto grid size-12 place-items-center rounded-2xl bg-primary/10 text-primary"><ShieldCheck size={22}/></div><h1 className="mt-5 text-2xl font-semibold">Private roadmap</h1><p className="mt-2 text-sm text-muted-foreground">This roadmap is shared directly with specific Roadmap accounts. No password is required.</p>{loading?<p className="mt-6 text-sm text-muted-foreground">Checking access…</p>:status===401?<Button className="mt-6 w-full" onClick={()=>{location.href=`/auth/login?next=${encodeURIComponent(location.pathname)}`}}><LogIn size={15}/> Sign in to continue</Button>:<div className="mt-6 flex items-start gap-2 rounded-xl border border-amber-500/20 bg-amber-500/5 p-3 text-left text-sm text-amber-800 dark:text-amber-200"><AlertCircle size={17} className="mt-0.5 shrink-0"/><span>You do not have access yet. Ask the roadmap owner to share it with your email or User ID.</span></div>}</Card></main>}
