'use client'; import {ReactNode} from 'react';
export function Button({children,className='',variant='primary',...p}:{children:ReactNode;className?:string;variant?:'primary'|'ghost'|'outline'}&React.ButtonHTMLAttributes<HTMLButtonElement>){return <button className={`rounded-xl px-3.5 py-2 text-sm font-medium transition ${variant==='primary'?'bg-[hsl(var(--accent))] text-white shadow-sm hover:opacity-90':variant==='outline'?'border border-[hsl(var(--line))] bg-[hsl(var(--card))] hover:bg-slate-50 dark:hover:bg-slate-900':'hover:bg-slate-100 dark:hover:bg-slate-800'} ${className}`} {...p}>{children}</button>}
export function Card({children,className=''}:{children:ReactNode;className?:string}){return <div className={`rounded-2xl border border-[hsl(var(--line))] bg-[hsl(var(--card))] shadow-sm ${className}`}>{children}</div>}
export function Badge({children}:{children:ReactNode}){return <span className="rounded-full bg-slate-100 px-2 py-0.5 text-xs text-slate-600 dark:bg-slate-800 dark:text-slate-300">{children}</span>}
