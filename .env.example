# Aiven PostgreSQL — the only database used by Roadmap
DATABASE_URL=""

# Server-only signing key for login sessions. Generate 32+ random characters.
AUTH_SECRET=""

# Used to build password-reset links.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
