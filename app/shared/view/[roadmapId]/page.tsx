'use client';
import {useEffect,useState} from 'react';
import {AppShell} from '@/components/app-shell';
import {Card,Badge} from '@/components/ui';
export default function SharedView({params}:{params:Promise<{roadmapId:string}>}){const [data,setData]=useState<any>(null);const[error,setError]=useState('');useEffect(()=>{params.then(p=>fetch(`/api/shared/access?roadmapId=${p.roadmapId}`).then(async r=>{if(!r.ok)throw new Error(await r.text());return r.json()}).then(setData).catch(e=>setError(e.message)) )},[params]);if(error)return <AppShell><Card className="p-8">{error}</Card></AppShell>;if(!data)return <AppShell><Card className="p-8">Loading shared roadmap…</Card></AppShell>;const map=new Map(data.roadmap.topics.map((t:any)=>[t.id,{...t,children:[]}]));const roots:any[]=[];for(const t of data.roadmap.topics){const n=map.get(t.id)!;if(t.parentId&&map.has(t.parentId))map.get(t.parentId).children.push(n);else roots.push(n)}const sort=(a:any[])=>{a.sort((x,y)=>x.position-y.position);a.forEach(n=>sort(n.children))};sort(roots);function Row({n,depth=0}:{n:any;depth?:number}){return <div className="py-2" style={{paddingLeft:depth*22}}><div className="flex justify-between"><div><b>{n.title}</b><p className="text-xs text-slate-500">{n.progress}% · {n.id}</p></div><Badge>{n.status}</Badge></div>{n.resources?.map((r:any)=><a className="mt-1 block text-xs text-indigo-600" key={r.id} href={r.url} target="_blank">{r.title}</a>)}{n.children.map((c:any)=><Row key={c.id} n={c} depth={depth+1}/>)}</div>}
return <AppShell><header className="mb-6"><p className="text-xs uppercase tracking-wide text-indigo-500">Shared · {data.access}</p><h1 className="mt-2 text-3xl font-bold">{data.roadmap.title}</h1><p className="mt-1 text-slate-500">{data.roadmap.description}</p></header><Card className="p-6"><div className="divide-y divide-[hsl(var(--line))]">{roots.map(n=><Row key={n.id} n={n}/>)}</div></Card></AppShell>}
