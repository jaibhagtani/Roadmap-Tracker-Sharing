'use client';
import { FormEvent, useState } from 'react'; import Link from 'next/link'; import { AuthLinks, AuthMessage, AuthShell, useNextPath } from '@/components/auth-form'; import { Button } from '@/components/ui'; import { authAction } from '@/lib/auth-client';
export default function Login(){const next=useNextPath();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMsg('');try{await authAction('/api/auth/login',{email,password});location.href=next;}catch(err){setMsg(err instanceof Error?err.message:'Unable to login.');}finally{setLoading(false)}}return <AuthShell title="Welcome back" subtitle="Sign in to continue your learning journey." footer={<>New to Roadmap? <Link href="/auth/signup" className="text-indigo-600 hover:underline">Create your account</Link></>}><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-medium">Email<input autoComplete="email" required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="mt-1.5 w-full rounded-xl border border-[hsl(var(--line))] bg-transparent px-3 py-2.5"/></label><label className="block text-sm font-medium">Password<input autoComplete="current-password" required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password" className="mt-1.5 w-full rounded-xl border border-[hsl(var(--line))] bg-transparent px-3 py-2.5"/></label><Button disabled={loading} className="w-full">{loading?'Signing in…':'Login'}</Button><AuthMessage message={msg} error/></form><div className="mt-5"><AuthLinks/></div></AuthShell>}
