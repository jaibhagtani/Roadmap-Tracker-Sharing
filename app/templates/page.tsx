'use client';
import { useEffect, useState } from 'react';
import { AppShell } from '@/components/app-shell';
import { Card, Button } from '@/components/ui';
type Template={id:string;name:string;description:string;tree:any[]};

export default function Templates(){const [templates,setTemplates]=useState<Template[]>([]);const [busy,setBusy]=useState('');const [receiver,setReceiver]=useState('');async function load(){const r=await fetch('/api/templates');const j=await r.json();setTemplates(j.templates??[])}useEffect(()=>{load()},[]);async function shareTemplate(id:string,name:string){if(!receiver.trim())return alert('Enter the receiver User ID.');const r=await fetch('/api/share-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({scopeType:'template',templateId:id,receiverId:receiver.trim(),message:`You have been invited to use the ${name} template.`})});if(!r.ok)return alert(await r.text());setReceiver('');alert('Template share notification sent.')}async function useTemplate(id:string,name:string){setBusy(id);const r=await fetch('/api/templates',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({templateId:id})});setBusy('');if(!r.ok)return alert(await r.text());const j=await r.json();alert(`${name} roadmap created.`);location.href=`/roadmap?roadmapId=${j.roadmap.id}`}return <AppShell><header><h1 className="text-3xl font-bold">Templates</h1><p className="mt-1 text-slate-500">Templates create real database-backed roadmaps that you can edit immediately.</p></header><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{templates.map(t=><Card key={t.id} className="p-6"><div className="text-xs font-semibold uppercase tracking-wide text-indigo-500">Template</div><h2 className="mt-2 text-lg font-semibold">{t.name}</h2><p className="mt-2 text-sm text-slate-500">{t.description}</p><p className="mt-4 text-xs text-slate-500">{t.tree.length} top-level areas · unlimited editable nesting</p><Button className="mt-5 w-full" disabled={!!busy} onClick={()=>useTemplate(t.id,t.name)}>{busy===t.id?'Creating…':'Use template'}</Button><div className="mt-3 flex gap-2"><input value={receiver} onChange={e=>setReceiver(e.target.value)} placeholder="Receiver User ID" className="min-w-0 flex-1 rounded-xl border border-[hsl(var(--line))] bg-transparent px-3 py-2 text-xs"/><Button variant="outline" onClick={()=>shareTemplate(t.id,t.name)}>Share</Button></div></Card>)}</div></AppShell>}
