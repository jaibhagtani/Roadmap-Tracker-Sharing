'use client';
import { useEffect, useState } from 'react';
import { AppShell } from '@/components/app-shell';
import { Card, Button } from '@/components/ui';
export default function Settings(){const[user,setUser]=useState<any>(null);const[profile,setProfile]=useState({fullName:'',avatarUrl:'',bio:''});const[busy,setBusy]=useState(false);useEffect(()=>{fetch('/api/me').then(r=>r.json()).then(j=>{setUser(j.user);setProfile({fullName:j.profile?.fullName??'',avatarUrl:j.profile?.avatarUrl??'',bio:j.profile?.bio??''})})},[]);async function save(){setBusy(true);const r=await fetch('/api/me',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(profile)});setBusy(false);if(!r.ok)return alert(await r.text());alert('Profile saved.')}return <AppShell><header className="mb-6"><h1 className="text-3xl font-bold">Settings</h1><p className="mt-1 text-slate-500">Your account identity and profile.</p></header><div className="grid gap-5 md:grid-cols-2"><Card className="p-6"><h2 className="font-semibold">Roadmap User ID</h2><p className="mt-2 text-sm text-slate-500">Give this ID to another Roadmap user when they want to share a roadmap with you.</p><div className="mt-4 flex gap-2"><input readOnly value={user?.id??''} className="min-w-0 flex-1 rounded-xl border border-[hsl(var(--line))] bg-slate-50 px-3 py-2 font-mono text-xs dark:bg-slate-900"/><Button variant="outline" onClick={()=>navigator.clipboard?.writeText(user?.id??'')}>Copy ID</Button></div><p className="mt-3 text-xs text-slate-500">Email: {user?.email??'—'}</p></Card><Card className="p-6"><h2 className="font-semibold">Profile</h2><div className="mt-4 space-y-3"><label className="block text-sm font-medium">Name<input value={profile.fullName} onChange={e=>setProfile({...profile,fullName:e.target.value})} className="mt-1 w-full rounded-xl border border-[hsl(var(--line))] bg-transparent px-3 py-2"/></label><label className="block text-sm font-medium">Avatar URL<input value={profile.avatarUrl} onChange={e=>setProfile({...profile,avatarUrl:e.target.value})} className="mt-1 w-full rounded-xl border border-[hsl(var(--line))] bg-transparent px-3 py-2"/></label><label className="block text-sm font-medium">Bio<textarea value={profile.bio} onChange={e=>setProfile({...profile,bio:e.target.value})} className="mt-1 h-28 w-full rounded-xl border border-[hsl(var(--line))] bg-transparent p-3"/></label><Button onClick={save} disabled={busy}>{busy?'Saving…':'Save profile'}</Button></div></Card></div></AppShell>}
