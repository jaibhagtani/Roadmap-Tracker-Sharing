import { NextResponse } from 'next/server';
import { withRls, errorResponse } from '@/lib/db';
import { requireUser } from '@/lib/server-auth';
import { z } from 'zod';
const schema=z.object({roadmapId:z.string().uuid().nullable().default(null),topicId:z.string().uuid().nullable().default(null),todoDate:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),title:z.string().trim().min(1).max(300),notes:z.string().max(5000).default(''),completed:z.boolean().default(false)});
const dateOnly=(s:string)=>new Date(`${s}T00:00:00.000Z`);
export async function GET(req:Request){try{const user=await requireUser();const q=new URL(req.url).searchParams;const date=q.get('date');const from=q.get('from');const to=q.get('to');const todos=await withRls(user.id,tx=>tx.todo.findMany({where:{ownerId:user.id,...(date?{todoDate:dateOnly(date)}:{...(from||to?{todoDate:{...(from?{gte:dateOnly(from)}:{}),...(to?{lte:dateOnly(to)}:{})}}:{})})},orderBy:[{todoDate:'asc'},{position:'asc'},{createdAt:'asc'}]}));return NextResponse.json({todos});}catch(e){return errorResponse(e)}}
export async function POST(req:Request){try{const user=await requireUser();const input=schema.parse(await req.json());const todo=await withRls(user.id,async tx=>{if(input.roadmapId){const r=await tx.roadmap.findFirst({where:{id:input.roadmapId,ownerId:user.id}});if(!r)throw new Error('NOT_FOUND')}if(input.topicId){const t=await tx.topic.findFirst({where:{id:input.topicId,roadmap:{ownerId:user.id}}});if(!t)throw new Error('NOT_FOUND')}const max=await tx.todo.aggregate({where:{ownerId:user.id,todoDate:dateOnly(input.todoDate)},_max:{position:true}});return tx.todo.create({data:{ownerId:user.id,roadmapId:input.roadmapId,topicId:input.topicId,todoDate:dateOnly(input.todoDate),title:input.title,notes:input.notes,completed:input.completed,position:(max._max.position??-1)+1}})});return NextResponse.json({todo},{status:201})}catch(e){return errorResponse(e)}}
