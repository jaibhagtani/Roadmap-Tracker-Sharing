import { NextResponse } from 'next/server'; import { z } from 'zod'; import { db } from '@/lib/db'; import { createSession, hashPassword, verifyPassword, SESSION_COOKIE } from '@/lib/server-auth';
const schema=z.object({email:z.string().email(),password:z.string().min(8)});
export async function POST(req:Request){try{const i=schema.parse(await req.json());const user=await db.user.findUnique({where:{email:i.email.toLowerCase()}});if(!user||!verifyPassword(i.password,user.passwordHash))return NextResponse.json({error:'Invalid email or password.'},{status:401});const {token,expiresAt}=await createSession(user.id);const res=NextResponse.json({user:{id:user.id,email:user.email,fullName:user.fullName}});res.cookies.set(SESSION_COOKIE,token,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',expires:expiresAt});return res}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to login.'},{status:400})}}
